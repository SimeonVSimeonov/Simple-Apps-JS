{{> nav}}
<section id="viewCatalog" class="background-img">
    <a href="#/addFlight" class="add-flight">Add Flight +</a>

    <div class="added-flights">
        {{#each flights}}
            <a href="#/flightDetails/{{_id}}" class="added-flight">
                <img src="{{img}}" alt="" class="picture-added-flight">
                <h3>{{destination}}</h3>
                <span>from {{origin}} {{departureDate}}</span><span>{{date}}</span>
            </a>
        {{/each}}
    </div>
</section>